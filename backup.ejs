<style>
  .order-Details {
    background-color: #d4edda;
    border-radius: 25px;
    padding: 20px;
    margin: 20px;
  }

  .heading {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: row;
    gap: 20px;
  }



  .invoice {
    background-color: #007bff;
    color: white;
    border: none;
    padding: 5px 10px;
    border-radius: 4px;
    cursor: pointer;
  }

  .invoice:hover {
    background-color: #0056b3;
  }

  .deatils {
    border-radius: 25px;
    padding: 20px;
  }

  .order-info,
  .order-charges,
  .billing-address {
    background-color: #f9f9f9;
    padding: 20px;
    margin-bottom: 20px;
    border-radius: 10px;
    height: 100%;
  }

  .order-info h3,
  .order-charges h3,
  .billing-address h3 {
    margin-top: 0;
  }

  .order-info p,
  .order-charges p,
  .billing-address p {
    margin-bottom: 8px;
  }

  .order-info button {
    margin-top: 8px;
  }

  .order-info strong,
  .order-charges strong,
  .billing-address strong {
    font-weight: bold;
  }

  .progress-container {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin: 30px 0;
    padding: 20px 0;
  }

  .icon-circle {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background: #ccc;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
    color: white;
    transition: background 0.5s ease-in-out;
  }

  .completed {
    background: #28a745 !important;
  }

  .step-text {
    font-size: 14px;
    margin-top: 8px;
    color: #666;
    transition: color 0.5s ease-in-out;
  }

  .completed-text {
    color: #28a745 !important;
    font-weight: bold;
  }

  .progress-line {
    position: absolute;
    top: 25px;
    left: 0;
    width: 100%;
    height: 5px;
    background: #ccc;
    z-index: -1;
  }

  .completed-line {
    position: absolute;
    top: 25px;
    left: 0;
    height: 5px;
    background: #28a745;
    transition: width 0.5s ease-in-out;
  }

  .other-orders {
    border-radius: 25px;
    padding: 20px;
  }
</style>
<section class="mt-50 mb-50">
  <div class="container">
    <div class="row order-Details">
      <div class="col-md-12 text-center">
        <% if (!selectedItem) { %>
          <div class="alert alert-info" role="alert">
            No selected item found. Please select an item.
          </div>
          <% } else { %>
            <div class="heading">
              <h2 class="my-4"><i class="fas fa-check-circle" style="color: green;"></i> Order Details</h2>
              <% if (selectedItem.orderStatus==='delivered' ) { %>
                <button id="invoiceButton" class="btn-sm invoice">Download Invoice</button>
                <% } %>
            </div>

            <img src="/uploads/product/resized/<%= selectedItem.productId.productImage[0] %>"
              style="width: 200px;height: 200px;" alt="<%= selectedItem.productId.name %>">
            <p>
              <%= selectedItem.productId.name %> (<%= selectedItem.price.toFixed(2) %> * <%= selectedItem.quantity %>)
            </p>
            <p> ₹<%= (selectedItem.price.toFixed(2)* selectedItem.quantity) %>
            </p>

            <% const orderSteps=[ { status: "ordered" , icon: "fa-solid fa-cart-shopping" }, { status: "shipped" ,
              icon: "fa-solid fa-truck" }, { status: "out for Delivery" , icon: "fa-solid fa-box" }, {
              status: "delivered" , icon: "fa-solid fa-check-circle" } ]; const currentStatus=selectedItem.orderStatus;
              const currentIndex=orderSteps.findIndex(step=> step.status ===
              currentStatus);
              %>

              <!-- Bootstrap & Font Awesome -->
              <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">

              <div class="container mt-4">
                <div class="row justify-content-center">
                  <div class="col-md-8">
                    <div class="progress-container d-flex justify-content-between position-relative">
                      <% orderSteps.forEach((step, index)=> { %>
                        <div class="step text-center">
                          <div class="icon-circle <%= index <= currentIndex ? 'completed' : '' %>">
                            <i class="<%= step.icon %>"></i>
                          </div>
                          <p class="step-text <%= index <= currentIndex ? 'completed-text' : '' %>">
                            <%= step.status %>
                          </p>
                        </div>
                        <% }); %>
                          <div class="progress-line"></div>
                          <div class="progress-line completed-line"
                            style="width: '<%= (currentIndex / (orderSteps.length - 1)) * 100 %>%';"></div>
                    </div>
                  </div>
                </div>
              </div>




              <td class="align-middle">
                <% if (selectedItem.orderStatus==='delivered' ) { %>
                  <button class="btn-warnings return-product" data-item-id="<%= selectedItem._id %>">Return
                    Product</button>
                  <% } else if (selectedItem.orderStatus==='cancelled' ) { %>
                    <span class="text-danger fw-bold">Cancelled</span>
                    <% } else if (selectedItem.orderStatus==='awaiting cancellation approval' ) { %>
                      <span class="text-info fw-bold">Awaiting Cancellation Approval</span>
                      <% } else if (selectedItem.orderStatus==='rejected' ) { %>
                        <div class="d-flex flex-column align-items-center">
                          <span class="text-danger fw-bold fs-5">Cancellation Rejected</span>
                          <button class="btn-warnings cancel-product mt-2" data-item-id="<%= selectedItem._id %>">
                            Cancel Product
                          </button>
                        </div>

                        <% } else if (selectedItem.orderStatus==='returned' ) { %>
                          <span class="text-danger fw-bold">Returned</span>
                          <% } else if (selectedItem.orderStatus==='refund initiated' ) { %>
                            <span class="text-info fw-bold">Refund Initiated</span>
                            <% } else if (selectedItem.orderStatus==='refunded' ) { %>
                              <span class="text-success fw-bold">Refunded</span>
                              <% } else if (selectedItem.orderStatus==='failed' ) { %>
                                <span class="text-danger fw-bold">Failed</span><br>
                                <button class="btn-warnings cancel-product"
                                  data-item-id="<%= selectedItem._id %>">Cancel Product</button>
                                <% } else { %>
                                  <button class="btn-warnings cancel-product"
                                    data-item-id="<%= selectedItem._id %>">Cancel Product</button>
                                  <% } %>
              </td>



              <% } %>




      </div>
    </div>
    <div class="row deatils">
      <% if (order.shippingCharge> 0 || order.offerDiscount > 0 || order.couponDiscount > 0) { %>
        <div class="col-md-4">
          <div class="order-info">
            <h3>Order Information</h3>
            <hr>
            <p><strong>Order ID:</strong>
              <%= order.ordersId %>
            </p>
            <p><strong>Order Date:</strong>
              <%= order.date.toDateString() %>
            </p>
            <p><strong>Expected Delivery Date:</strong>
              <%= new Date(order.expectedDelivery).toDateString() %>
            </p>

            <p><strong>Order Status:</strong>
              <%= order.items[0].orderStatus %>
            </p>
            <p><strong>Total:</strong> ₹<%= (selectedItem.price.toFixed(2) * selectedItem.quantity) %>
          </div>
        </div>
        <div class="col-md-4">
          <div class="order-charges">
            <h3>Payment Deatils</h3>
            <hr>
            <p><strong>Payment Method:</strong>
              <%= order.paymentMethod %>
            </p>
            <p><strong>Payment Status:</strong>
              <% if (order.status==='retry' && selectedItem.orderStatus !='cancelled' ) { %>
                <button id="retryButton" class="btn-sm btn-primary">Retry Payment</button>
                <% } else { %>
                  <%= order.status %>
                    <% } %>
            </p>
            <br>
            <h3> Charges & Discount</h3>
            <hr>
            <% if (order.shippingCharge> 0) { %>
              <p><strong>Shipping Charges:</strong>
                <%= order.shippingCharge %>
              </p>
              <% } %>
                <% if (order.offerDiscount> 0) { %>
                  <p><strong>Offer Discount:</strong>
                    <%= order.offerDiscount.toFixed(2) %>
                  </p>
                  <% } %>
                    <% if (order.couponDiscount> 0) { %>
                      <p><strong>Coupon Discount:</strong>
                        <%= order.couponDiscount.toFixed(2) %>
                      </p>
                      <% } %>
          </div>
        </div>

        <div class="col-md-4">
          <div class="billing-address">
            <h3>Billing Address</h3>
            <hr>
            <p><strong>Name:</strong>
              <%= order.deliveryAddress.fullName %>
            </p>

            <p><strong>Phone Number:</strong>
              <%= order.deliveryAddress.mobileNumber %>
            </p>
            <p><strong>Address:</strong>
              <%= order.deliveryAddress.houseNo %>, <%= order.deliveryAddress.area %>, <%= order.deliveryAddress.city %>
            </p>
            <p> <strong>State:</strong>
              <%= order.deliveryAddress.state %>
            </p>
            <p><strong>Pincode:</strong>
              <%= order.deliveryAddress.pincode %>
            </p>
          </div>
        </div>
        <% } else { %>
          <div class="col-md-6">
            <div class="order-info">
              <h3>Order Information</h3>
              <hr>
              <p><strong>Order ID:</strong>
                <%= order.ordersId %>
              </p>
              <p><strong>Order Date:</strong>
                <%= order.date.toDateString() %>
              </p>
              <p><strong>Expected Delivery Date:</strong>
                <%= new Date(order.expectedDelivery).toDateString() %>
              </p>
              <p><strong>Payment Method:</strong>
                <%= order.paymentMethod %>
              </p>
              <p><strong>Payment Status:</strong>
                <% if (order.status==='retry' && selectedItem.orderStatus !='cancelled' ) { %>
                  <button id="retryButton" class="btn-sm btn-primary">Retry Payment</button>
                  <% } else { %>
                    <%= order.status %>
                      <% } %>
              </p>
              <p><strong>Order Status:</strong>
                <%= order.items[0].orderStatus %>
              </p>
              <p><strong>Total:</strong> ₹<%= (selectedItem.price.toFixed(2)* selectedItem.quantity) %>
            </div>
          </div>
          <div class="col-md-6">
            <div class="billing-address">
              <h3>Billing Address</h3>
              <hr>
              <p><strong>Name:</strong>
                <%= order.deliveryAddress.fullName %>
              </p>

              <p><strong>Phone Number:</strong>
                <%= order.deliveryAddress.mobileNumber %>
              </p>
              <p><strong>Address:</strong>
                <%= order.deliveryAddress.houseNo %>, <%= order.deliveryAddress.area %>, <%= order.deliveryAddress.city
                      %>, <%= order.deliveryAddress.state %>
              </p>
              <p><strong>Pincode:</strong>
                <%= order.deliveryAddress.pincode %>
              </p>
            </div>
          </div>
          <% } %>
    </div>

    <% if (OtherOrders.length> 0) { %>
      <div class="row other-orders">
        <div class="col-md-12">
          <h3>Other Orders:</h3>
          <div class="table-responsive">
            <table class="table " style="text-align: center;">
              <thead>
                <tr>
                  <th>Product Image</th>
                  <th>Name</th>
                  <th>Total</th>
                  <th>Order status</th>
                </tr>
              </thead>
              <tbody>
                <% OtherOrders.forEach(item=> { %>
                  <tr>
                    <td>
                      <a href="/order/orderDeatil/<%= order._id %>?productId=<%=  item.productId._id %>">
                        <img src="/uploads/product/resized/<%= item.productId.productImage[0] %>"
                          style="width: 150px;height: 150px;" alt="<%= item.productId.name %>">
                      </a>
                    </td>
                    <td>
                      <a href="/order/orderDeatil/<%= order._id %>?productId=<%=  item.productId._id %>">
                        <%= item.productId.name %> (<%= item.price.toFixed(2) %>*<%= item.quantity %> )
                      </a>
                    </td>
                    <td> ₹<%= item.price.toFixed(2) * item.quantity %>
                    </td>
                    <td>
                      <%= item.orderStatus %>
                    </td>
                  </tr>

                  <% }); %>
                    <tr>
                      <td colspan="4" class="text-end">
                        Total:₹<%= order.totalAmount.toFixed(2) %>
                      </td>
                    </tr>
              </tbody>
            </table>
          </div>
        </div>
      </div>
      <% } %>
  </div>
  <!-- Modal for cancellation and return -->
  <div class="modal fade" id="cancellationModal" tabindex="-1" aria-labelledby="cancellationModalLabel"
    aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="cancellationModalLabel">Action Title</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <form id="cancellationForm">
            <div class="mb-3">
              <label for="cancellationReason" class="form-label">Reason</label>
              <textarea id="cancellationReason" class="form-control" rows="4"
                placeholder="Enter reason for action"></textarea>
              <div id="cancellationReasonError" class="invalid-feedback"></div>
            </div>
          </form>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn-secondary" data-bs-dismiss="modal">Close</button>
          <button type="button" class="btn-primary" id="confirmCancellation">Confirm</button>
        </div>
      </div>
    </div>
  </div>
</section>





<div class="col-md-6 col-lg-4 mb-4">
  <div class="card order-card shadow-sm h-100 hover-effect">
    <div class="image-container position-relative">
      <img src="/uploads/product/resized/<%= item.productId.productImage[0] %>"
        class="card-img-top order-img img-fluid p-3" alt="<%= item.productId.name %>"
        style="height: 250px; object-fit: contain;">
      <div class="position-absolute top-0 end-0 m-3">
        <span class="badge rounded-pill p-2" style="background-color: #088178;">
          <%= item.quantity %> × ₹<%= item.price.toFixed(2) %>
        </span>
      </div>
    </div>

    <div class="card-body pt-0">
      <div class="d-flex justify-content-center align-items-center mb-2">
        <h5 class="card-title mb-0 text-truncate">
          <%= item.productId.name %>
        </h5>
      </div>
      <span class="d-flex justify-content-center align-items-center mb-2">#
        <%= order.ordersId %>
      </span>
      <span class="text-muted small">
        <i class="fas fa-truck-clock me-1"></i>
        <%= order.expectedDelivery.toDateString() %>
      </span>
    </div>

  </div>

</div>
</div>
</div>


http://localhost:3434/order/orderConfirmation/67d82a4d58bb07ff782eab17








<style>
  .order-details-container {
    background: #ffffff;
    border-radius: 15px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    padding: 2rem;
    margin: 2rem 0;
  }


  .order-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid #eaeaea;
  }

  .btn-invoice {
    color: white;
    padding: 0.5rem 1.25rem;
    border-radius: 25px;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    transition: all 0.3s ease;
  }

  .btn-invoice:hover {
    background: #046963;
    transform: translateY(-1px);
  }


  .deatils {
    border-radius: 25px;
    padding: 2rem;
    margin: 2rem 0;
  }

  .order-info,
  .order-charges,
  .billing-address {
    background-color: #f9f9f9;
    padding: 20px;
    margin-bottom: 20px;
    border-radius: 10px;
    height: 100%;
  }

  .order-info h3,
  .order-charges h3,
  .billing-address h3 {
    margin-top: 0;
  }

  .order-info p,
  .order-charges p,
  .billing-address p {
    margin-bottom: 8px;
  }

  .order-info button {
    margin-top: 8px;
  }

  .order-info strong,
  .order-charges strong,
  .billing-address strong {
    font-weight: bold;
  }

  .progress-container {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin: 20px 0;
    padding: 20px 0;
    border: 2px solid black;
  }

  .icon-circle {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background: #ccc;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
    color: white;
    transition: background 0.5s ease-in-out;
  }

  .completed {
    background: #088178 !important;
  }

  .step-text {
    font-size: 14px;
    margin-top: 8px;
    color: #666;
    transition: color 0.5s ease-in-out;
  }

  .completed-text {
    color: #088178 !important;
    font-weight: bold;
  }

  .progress-line {
    position: absolute;
    top: 25px;
    left: 0;
    width: 100%;
    height: 5px;
    background: #ccc;
    z-index: 1;
  }

  .completed-line {
    position: absolute;
    top: 25px;
    left: 0;
    height: 5px;
    background: #088178;
    transition: width 0.5s ease-in-out;
  }

  .other-orders {
    border-radius: 25px;
  }
</style>
<section class="mt-50 mb-50">
  <div class="container">
    <div class="order-details-container">
      <div class="col-md-12 text-center">
        <% if (!selectedItem) { %>
          <div class="alert alert-info" role="alert">
            No selected item found. Please select an item.
          </div>
          <% } else { %>
            <div class="order-header">
              <h2 class="mb-0"><i class="fas fa-check-circle text-success me-2"></i>Order Details</h2>
              <% if (selectedItem.orderStatus==='delivered' ) { %>
                <button id="invoiceButton" class="btn btn-invoice">
                  <i class="fas fa-download me-1"></i>Download Invoice
                </button>
                <% } %>
            </div>

            <div class="col-12 d-flex justify-content-center">
              <div class=" hover-effect rounded-4 border-0" style="max-width: 350px;">
                <div class="image-container position-relative text-center p-4">
                  <img src="/uploads/product/resized/<%= selectedItem.productId.productImage[0] %>"
                    class="img-fluid rounded-3 shadow-sm" style="width: 100%; height: 220px; object-fit: cover;"
                    alt="<%= selectedItem.productId.name %>">
                  <div class="position-absolute top-0 end-0 m-2">
                    <span class="badge rounded-pill px-3 py-2 shadow-sm"
                      style="background-color: #088178; font-size: 0.85rem; font-weight: 500;">
                      <%= selectedItem.quantity %> × ₹<%= selectedItem.price.toFixed(2) %>
                    </span>
                  </div>
                </div>
                <div class="card-body text-center">
                  <h5 class="fw-bold text-dark fs-5 mb-2">
                    <%= selectedItem.productId.name %>
                  </h5>
                  <p class="text-muted small fs-6 mb-3">
                    <strong>Total: ₹<%= selectedItem.total.toFixed(2) %></strong>
                  </p>

                </div>
              </div>
            </div>




            <% const orderSteps=[ { status: "ordered" , icon: "fa-solid fa-cart-shopping" }, { status: "shipped" ,
              icon: "fa-solid fa-truck" }, { status: "out for Delivery" , icon: "fa-solid fa-box" }, {
              status: "delivered" , icon: "fa-solid fa-check-circle" } ]; const currentStatus=selectedItem.orderStatus;
              const currentIndex=orderSteps.findIndex(step=> step.status
              ===
              currentStatus);
              %>

              <!-- Bootstrap & Font Awesome -->
              <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">

              <div class="container mt-4">
                <div class="row justify-content-center">
                  <div class="col-md-8">
                    <div class="progress-container d-flex justify-content-between position-relative">
                      <% orderSteps.forEach((step, index)=> { %>
                        <div class="step text-center">
                          <div class="icon-circle <%= index <= currentIndex ? 'completed' : '' %>">
                            <i class="<%= step.icon %>"></i>
                          </div>
                          <p class="step-text <%= index <= currentIndex ? 'completed-text' : '' %>">
                            <%= step.status %>
                          </p>
                        </div>
                        <% }); %>
                          <div class="progress-line"></div>
                          <div class="progress-line completed-line"
                            style="width: '<%= (currentIndex / (orderSteps.length - 1)) * 100 %>%';"></div>
                    </div>
                  </div>
                </div>
              </div>




              <td class="align-middle">
                <% if (selectedItem.orderStatus==='delivered' ) { %>
                  <button class="btn-warnings return-product" data-item-id="<%= selectedItem._id %>">Return
                    Product</button>
                  <% } else if (selectedItem.orderStatus==='cancelled' ) { %>
                    <span class="text-danger fw-bold">Cancelled</span>
                    <% } else if (selectedItem.orderStatus==='awaiting cancellation approval' ) { %>
                      <span class="text-info fw-bold">Awaiting Cancellation Approval</span>
                      <% } else if (selectedItem.orderStatus==='rejected' ) { %>
                        <div class="d-flex flex-column align-items-center">
                          <span class="text-danger fw-bold fs-5">Cancellation Rejected</span>
                          <button class="btn-warnings cancel-product mt-2" data-item-id="<%= selectedItem._id %>">
                            Cancel Product
                          </button>
                        </div>

                        <% } else if (selectedItem.orderStatus==='returned' ) { %>
                          <span class="text-danger fw-bold">Returned</span>
                          <% } else if (selectedItem.orderStatus==='refund initiated' ) { %>
                            <span class="text-info fw-bold">Refund Initiated</span>
                            <% } else if (selectedItem.orderStatus==='refunded' ) { %>
                              <span class="text-success fw-bold">Refunded</span>
                              <% } else if (selectedItem.orderStatus==='failed' ) { %>
                                <span class="text-danger fw-bold">Failed</span><br>
                                <button class="btn-warnings cancel-product"
                                  data-item-id="<%= selectedItem._id %>">Cancel Product</button>
                                <% } else { %>
                                  <button class="btn-warnings cancel-product"
                                    data-item-id="<%= selectedItem._id %>">Cancel Product</button>
                                  <% } %>
              </td>



              <% } %>




      </div>
    </div>
    <div class="row deatils">
      <% if (order.shippingCharge> 0 || order.offerDiscount > 0 || order.couponDiscount > 0) { %>
        <div class="col-md-4">
          <div class="order-info">
            <h3>Order Information</h3>
            <hr>
            <p><strong>Order ID:</strong>
              <%= order.ordersId %>
            </p>
            <p><strong>Order Date:</strong>
              <%= order.date.toDateString() %>
            </p>
            <p><strong>Expected Delivery Date:</strong>
              <%= new Date(order.expectedDelivery).toDateString() %>
            </p>

            <p><strong>Order Status:</strong>
              <%= order.items[0].orderStatus %>
            </p>
            <p><strong>Total:</strong> ₹<%= (selectedItem.price.toFixed(2) * selectedItem.quantity) %>
          </div>
        </div>
        <div class="col-md-4">
          <div class="order-charges">
            <h3>Payment Deatils</h3>
            <hr>
            <p><strong>Payment Method:</strong>
              <%= order.paymentMethod %>
            </p>
            <p><strong>Payment Status:</strong>
              <% if (order.status==='retry' && selectedItem.orderStatus !='cancelled' ) { %>
                <button id="retryButton" class="btn-sm btn-primary">Retry Payment</button>
                <% } else { %>
                  <%= order.status %>
                    <% } %>
            </p>
            <br>
            <h3> Charges & Discount</h3>
            <hr>
            <% if (order.shippingCharge> 0) { %>
              <p><strong>Shipping Charges:</strong>
                <%= order.shippingCharge %>
              </p>
              <% } %>
                <% if (order.offerDiscount> 0) { %>
                  <p><strong>Offer Discount:</strong>
                    <%= order.offerDiscount.toFixed(2) %>
                  </p>
                  <% } %>
                    <% if (order.couponDiscount> 0) { %>
                      <p><strong>Coupon Discount:</strong>
                        <%= order.couponDiscount.toFixed(2) %>
                      </p>
                      <% } %>
          </div>
        </div>

        <div class="col-md-4">
          <div class="billing-address">
            <h3>Billing Address</h3>
            <hr>
            <p><strong>Name:</strong>
              <%= order.deliveryAddress.fullName %>
            </p>

            <p><strong>Phone Number:</strong>
              <%= order.deliveryAddress.mobileNumber %>
            </p>
            <p><strong>Address:</strong>
              <%= order.deliveryAddress.houseNo %>, <%= order.deliveryAddress.area %>, <%= order.deliveryAddress.city %>
            </p>
            <p> <strong>State:</strong>
              <%= order.deliveryAddress.state %>
            </p>
            <p><strong>Pincode:</strong>
              <%= order.deliveryAddress.pincode %>
            </p>
          </div>
        </div>
        <% } else { %>
          <div class="col-md-6">
            <div class="order-info">
              <h3>Order Information</h3>
              <hr>
              <p><strong>Order ID:</strong>
                <%= order.ordersId %>
              </p>
              <p><strong>Order Date:</strong>
                <%= order.date.toDateString() %>
              </p>
              <p><strong>Expected Delivery Date:</strong>
                <%= new Date(order.expectedDelivery).toDateString() %>
              </p>
              <p><strong>Payment Method:</strong>
                <%= order.paymentMethod %>
              </p>
              <p><strong>Payment Status:</strong>
                <% if (order.status==='retry' && selectedItem.orderStatus !='cancelled' ) { %>
                  <button id="retryButton" class="btn-sm btn-primary">Retry Payment</button>
                  <% } else { %>
                    <%= order.status %>
                      <% } %>
              </p>
              <p><strong>Order Status:</strong>
                <%= order.items[0].orderStatus %>
              </p>
              <p><strong>Total:</strong> ₹<%= (selectedItem.price.toFixed(2)* selectedItem.quantity) %>
            </div>
          </div>
          <div class="col-md-6">
            <div class="billing-address">
              <h3>Billing Address</h3>
              <hr>
              <p><strong>Name:</strong>
                <%= order.deliveryAddress.fullName %>
              </p>

              <p><strong>Phone Number:</strong>
                <%= order.deliveryAddress.mobileNumber %>
              </p>
              <p><strong>Address:</strong>
                <%= order.deliveryAddress.houseNo %>, <%= order.deliveryAddress.area %>, <%= order.deliveryAddress.city
                      %>, <%= order.deliveryAddress.state %>
              </p>
              <p><strong>Pincode:</strong>
                <%= order.deliveryAddress.pincode %>
              </p>
            </div>
          </div>
          <% } %>
    </div>

    <% if (OtherOrders.length> 0) { %>
      <div class="row other-orders">
        <div class="col-md-12">
          <h3>Other Orders:</h3>
          <div class="table-responsive">
            <table class="table " style="text-align: center;">
              <thead>
                <tr>
                  <th>Product Image</th>
                  <th>Name</th>
                  <th>Total</th>
                  <th>Order status</th>
                </tr>
              </thead>
              <tbody>
                <% OtherOrders.forEach(item=> { %>
                  <tr>
                    <td>
                      <a href="/order/orderDeatil/<%= order._id %>?productId=<%=  item.productId._id %>">
                        <img src="/uploads/product/resized/<%= item.productId.productImage[0] %>"
                          style="width: 150px;height: 150px;" alt="<%= item.productId.name %>">
                      </a>
                    </td>
                    <td>
                      <a href="/order/orderDeatil/<%= order._id %>?productId=<%=  item.productId._id %>">
                        <%= item.productId.name %> (<%= item.price.toFixed(2) %>*<%= item.quantity %> )
                      </a>
                    </td>
                    <td> ₹<%= item.price.toFixed(2) * item.quantity %>
                    </td>
                    <td>
                      <%= item.orderStatus %>
                    </td>
                  </tr>

                  <% }); %>
                    <tr>
                      <td colspan="4" class="text-end">
                        Total:₹<%= order.totalAmount.toFixed(2) %>
                      </td>
                    </tr>
              </tbody>
            </table>
          </div>
        </div>
      </div>
      <% } %>
  </div>
  <!-- Modal for cancellation and return -->
  <div class="modal fade" id="cancellationModal" tabindex="-1" aria-labelledby="cancellationModalLabel"
    aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="cancellationModalLabel">Action Title</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <form id="cancellationForm">
            <div class="mb-3">
              <label for="cancellationReason" class="form-label">Reason</label>
              <textarea id="cancellationReason" class="form-control" rows="4"
                placeholder="Enter reason for action"></textarea>
              <div id="cancellationReasonError" class="invalid-feedback"></div>
            </div>
          </form>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn-secondary" data-bs-dismiss="modal">Close</button>
          <button type="button" class="btn-primary" id="confirmCancellation">Confirm</button>
        </div>
      </div>
    </div>
  </div>
</section>